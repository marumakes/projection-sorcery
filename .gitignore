# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Generated debug/run outputs (regenerate by running the pipeline)
debug_output/*/*
!debug_output/*/.gitkeep

# YOLO weights (downloaded automatically by ultralytics on first run)
*.pt

# Stock/source footage used to generate demo assets - licensed for reuse, not
# for standalone redistribution, so it stays local rather than in the repo.
demo-video.mp4
